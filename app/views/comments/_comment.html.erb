<div>
  <h4>Comment by <%= comment.author %></h4>
  <p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</p>
  <p class="comment"><%= comment.body %></p>
  <%= link_to "delete", [comment.article, comment], method: :delete, data: {confirm: "Really delete this comment?"} %>
</div>